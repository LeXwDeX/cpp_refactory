# 接缝分析：{目标}

> 文件：`{文件路径}`
> 行范围：L{起始}-L{结束}（如果指定）
> 日期：{YYYY-MM-DD}
> 分析工具：cpp-seam-finder.sh + 手动补充

---

## 1. 全局变量接缝

### 非 const 全局变量

| 变量名 | 类型 | 位置 | 读取方 | 修改方 | 线程安全 |
|---|---|---|---|---|---|
| {名称} | {类型} | {文件:行号} | {列出} | {列出} | {是/否/未知} |

### extern 声明

| 变量名 | 声明位置 | 定义位置 | 用途 |
|---|---|---|---|
| {名称} | {头文件:行号} | {源文件:行号} | {描述} |

---

## 2. 宏丛林

### 条件编译块

| 宏名 | 类型 | 控制行范围 | 功能 | 嵌套深度 |
|---|---|---|---|---|
| {名称} | ifdef/ifndef/if defined | L{start}-L{end} | {描述} | {N} |

### 功能性宏

| 宏名 | 定义位置 | 参数 | 用途 | 副作用 |
|---|---|---|---|---|
| {名称} | {文件:行号} | {参数列表} | {描述} | {有/无} |

---

## 3. 单例

| 类名 | 模式 | 位置 | 线程安全 |
|---|---|---|---|
| {名称} | {Meyer's / 全局指针 / 其他} | {文件:行号} | {是/否} |

---

## 4. 裸指针

### new/delete 平衡

| 分配 | 位置 | 对应释放 | 位置 | 平衡 |
|---|---|---|---|---|
| `new {Type}` | {文件:行号} | `delete` | {文件:行号} | {是/否/不确定} |

### void* 使用

| 位置 | 用途 | 安全性 |
|---|---|---|
| {文件:行号} | {描述} | {安全/风险} |

---

## 5. 回调与函数指针

| 类型 | 名称 | 位置 | 注册/调用方式 |
|---|---|---|---|
| {C 函数指针 / std::function / typedef} | {名称} | {文件:行号} | {描述} |

---

## 6. 封装破坏

### friend 声明

| 源类 | friend 目标 | 位置 | 必要性 |
|---|---|---|---|
| {类名} | {friend 类/函数} | {文件:行号} | {必要/可消除} |

---

## 7. 类型转换风险

| 转换类型 | 位置 | 代码 | 风险等级 |
|---|---|---|---|
| {const_cast / reinterpret_cast / C风格} | {文件:行号} | {代码片段} | {高/中/低} |

---

## 8. UB 风险点

| 类型 | 位置 | 描述 | 缓解建议 |
|---|---|---|---|
| {迭代器失效 / 悬空引用 / 未初始化 / 整数溢出} | {文件:行号} | {具体描述} | {建议} |

---

## 隐式依赖总结

### 输入隐式依赖（此区域静默读取的外部状态）

```
{变量名} @ {文件:行号}  读取方式：{直接引用/通过函数}
```

### 输出副作用（此区域静默修改的外部状态）

```
{变量名} @ {文件:行号}  修改方式：{直接赋值/通过指针}
```

---

## 接缝分类优先级

| 优先级 | 接缝类型 | 原因 |
|---|---|---|
| P0 | {类型} | {为什么必须首先处理} |
| P1 | {类型} | {为什么重要} |
| P2 | {类型} | {可以稍后处理} |
