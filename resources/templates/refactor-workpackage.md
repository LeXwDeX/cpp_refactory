# 重构工作包：{分区名称}

> **子任务**：重构分区 P-{NNN} — {分区名称}
> **来源规划**：`state/PARTITION_LEDGER.md`
> **日期**：{YYYY-MM-DD}

---

## 上下文

**文件**：`{文件路径}`
**行范围**：L{起始}-L{结束}（{N} 行）
**C++ 标准**：{CPP_STANDARD}
**风险等级**：{低/中/高}

### 区域职责

{一段话描述该区域在整体架构中的角色}

### 已知接缝（来自 Phase 1）

- {全局变量 / 宏 / 单例}：{具体描述}

### 历史经验（来自 mempalace）

- {相关重构经验摘要 / 无}

---

## 允许文件边界

仅允许修改以下文件：

- `{文件路径 1}` — L{起始}-L{结束}
- `{文件路径 2}` — {说明}

**禁止触碰**：
- `{文件/模块}` — 原因：{不在本分区范围内}

---

## 重构步骤

### Step 1：{步骤描述}

**操作**：{具体操作}
**重构手法**：{Extract Method / Replace Temp / ...}

**版本兼容性**：
```
需要特性：{列出}
CPP_STANDARD：{版本}
判定：{兼容 / 降级为 → {替代方案}}
```

**verify**：`{验证命令}`

### Step 2：{步骤描述}

**操作**：{具体操作}
**verify**：`{验证命令}`

---

## C++ 专项约束（本工作包适用）

- [ ] 未引入 `std::shared_ptr`（除非原代码是引用计数的）
- [ ] 未增加不必要的虚函数调用
- [ ] 未增加不必要的堆分配
- [ ] 保留了所有条件编译路径
- [ ] 所有 `const_cast` / `reinterpret_cast` 已标注理由
- [ ] 无迭代器失效风险
- [ ] 无悬空引用 / use-after-free

---

## 验收标准

### 编译验证

```bash
{完整的编译命令}
```
期望结果：编译成功，无新增警告

### 测试验证

```bash
{测试命令}
```
期望结果：所有现有测试通过

### 静态分析

```bash
clang-tidy -p . {修改过的文件}
```

### 人工审查点

- [ ] `const` 标注是否完整
- [ ] 异常安全级别是否满足
- [ ] 所有权转移是否明确
- [ ] 是否有遗漏的全局变量副作用

---

## 返回格式

```
执行结论：{成功 / 部分成功 / 失败}
变更摘要：
  - {文件:行范围}：{做了什么改动}
残余风险：
  - {未解决的风险}
建议回流：
  - {是否需要更新分区规划 / 是否发现新接缝}
归档建议：
  - {可复用的经验}
工具缺口：
  - {如果发现了新的工具不足}
```
